<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetBox"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="horizontal"
    android:paddingHorizontal="12dp"
    android:paddingVertical="8dp"
    android:gravity="center_vertical">

    <!-- Squircle Album Art on Left -->
    <ImageView
        android:id="@+id/ivWidgetArt"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:src="@drawable/default_album_art"
        android:scaleType="centerCrop"
        android:background="@drawable/bg_widget_album_art"
        android:contentDescription="Album Art" />

    <!-- Center Column: Title/Artist & Clean Controls -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp"
        android:gravity="center_vertical">

        <!-- Song Title -->
        <TextView
            android:id="@+id/tvWidgetTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Song Title"
            android:textColor="@android:color/white"
            android:textSize="13.5sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Artist Name -->
        <TextView
            android:id="@+id/tvWidgetArtist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Artist Name"
            android:textColor="#B3FFFFFF"
            android:textSize="11.5sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Controls Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="2dp">

            <ImageButton
                android:id="@+id/btnWidgetPrev"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:padding="3dp"
                android:src="@drawable/ic_widget_prev"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:tint="@android:color/white"
                android:scaleType="fitCenter"
                android:contentDescription="Previous" />

            <ImageButton
                android:id="@+id/btnWidgetPlayPause"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_marginStart="10dp"
                android:padding="2dp"
                android:src="@drawable/ic_widget_play"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:tint="@android:color/white"
                android:scaleType="fitCenter"
                android:contentDescription="Play/Pause" />

            <ImageButton
                android:id="@+id/btnWidgetNext"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_marginStart="10dp"
                android:padding="3dp"
                android:src="@drawable/ic_widget_next"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:tint="@android:color/white"
                android:scaleType="fitCenter"
                android:contentDescription="Next" />

            <!-- RemoteViews Compliant FrameLayout Spacer -->
            <FrameLayout
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1" />

            <!-- Favorite Heart at the complete end -->
            <ImageButton
                android:id="@+id/btnWidgetFavorite"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_marginEnd="2dp"
                android:padding="4dp"
                android:src="@drawable/ic_favorite_border"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:tint="@android:color/white"
                android:scaleType="fitCenter"
                android:contentDescription="Favorite" />
        </LinearLayout>
    </LinearLayout>

</LinearLayout>
